# ⏰ 配置定时任务

## 📋 任务说明

| 配置项 | 值 |
|--------|-----|
| 任务名称 | 飞书工单每日总结 |
| 执行时间 | 每天 22:00（晚上 10 点） |
| 时区 | Asia/Shanghai |
| 功能 | 为每个已结束的工单生成独立总结文档 |

---

## 🚀 配置方法

### 方法一：运行脚本（推荐）

```bash
bash /home/admin/.openclaw/workspace/feishu-helpdesk/setup-cron.sh
```

### 方法二：手动添加

```bash
openclaw cron add \
  --name "飞书工单每日总结" \
  --schedule-cron "0 22 * * *" \
  --schedule-tz "Asia/Shanghai" \
  --payload-kind "systemEvent" \
  --payload-text "【定时任务】执行飞书工单每日总结 - 运行 node /home/admin/.openclaw/workspace/feishu-helpdesk/scripts/daily-summary.js" \
  --session-target "main"
```

---

## ✅ 验证配置

### 查看任务列表
```bash
openclaw cron list
```

预期输出：
```
飞书工单每日总结    0 22 * * *    enabled
```

### 测试执行
```bash
# 先获取任务 ID
openclaw cron list

# 执行测试（替换 <JOB_ID>）
openclaw cron run --job-id <JOB_ID>
```

### 查看执行历史
```bash
openclaw cron runs --job-id <JOB_ID>
```

---

## 📂 输出说明

### 输出目录结构
```
output/
└── summary-2026-03-02/          # 按日期分组
    ├── daily-overview.md        # 每日总览
    ├── ticket-T20260302001.md   # 工单 1 总结
    ├── ticket-T20260302002.md   # 工单 2 总结
    └── ...
```

### 工单总结文档格式
```markdown
# 工单总结：[标题]

## 📋 基本信息
| 字段 | 值 |
|------|-----|
| 工单号 | T20260302001 |
| 状态 | closed |
| 申请人 | 张三 |
| 处理人 | 李四 |
| 创建时间 | 2026-03-02 09:15 |
| 关闭时间 | 2026-03-02 10:30 |
| 处理时长 | 75 分钟 |

## 💬 对话摘要
（聊天记录摘要）

## ✅ 解决方案
（解决方案）
```

---

## ⚠️ 当前限制（阶段一）

### 已实现
- ✅ 定时任务框架
- ✅ 每日总结文档生成
- ✅ 每个工单独立文档
- ✅ 本地文件输出

### 待实现（需要权限）
- ⏳ 获取实际工单数据（需要 `helpdesk:ticket:read`）
- ⏳ 获取聊天记录（需要 `im:message:readonly` 增强）
- ⏳ 上传到知识库（需要 `wiki:wiki:write`）

---

## 📈 阶段二升级

申请以下权限后，可以实现完整功能：

| 权限 | 用途 |
|------|------|
| `helpdesk:ticket:read` | 读取工单列表和详情 |
| `helpdesk:ticket:write` | 更新工单状态 |
| `wiki:wiki:write` | 上传总结到知识库 |
| `im:message:readonly` | 获取完整聊天记录 |

---

## 🔧 故障排查

### 任务不执行
1. 检查任务状态：`openclaw cron list`
2. 确认任务已启用：`enabled: true`
3. 检查 OpenClaw 服务状态

### 输出为空
1. 检查是否有已结束的工单
2. 检查工单 API 连接（阶段二）
3. 查看日志文件

### 时区问题
确认定时任务时区为 `Asia/Shanghai`，否则执行时间会偏移。

---

*最后更新：2026-03-02*
